pipeline {
  agent any

  environment {
    AWS_REGION = 'eu-central-1'
    // ECR_REPO should match Terraform output: module.ecr.repository_url
    ECR_REPO   = 'REPLACE_ME_ECR_REPO_URL'
    IMAGE_TAG  = "${env.BUILD_NUMBER}"
  }

  stages {
    stage('Checkout') {
      steps {
        checkout scm
      }
    }

    stage('Build & Push (placeholder)') {
      steps {
        echo "Implement Kaniko/Kubernetes agent build, push to ECR, then update Helm values in charts repo."
      }
    }
  }
}
